{
  "name": "dice-match-server",
  "version": "1.0.0",
  "private": true,
  "description": "Turn-based multiplayer dice game server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/cosmos": "^4.5.1",
    "bcryptjs": "^2.4.3",
    "ws": "^8.18.0"
  }
}